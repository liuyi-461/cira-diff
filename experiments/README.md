# Run Artifacts

本目录保留 experiment-local config、logs、prediction arrays、metric tables 和小型可审阅 artifact。叙述性证据放在 `docs/experiments/`；大型 data/checkpoint 按项目数据政策置于 Git 外部。

当前目录只是 scaffold，不表示已经完成新的 model run。
