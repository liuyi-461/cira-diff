# Cloud-Object Metrics

候选指标包括 object area、centroid/displacement、overlap、intensity、growth/decay rate、lifetime 和 organization。

状态：`PLANNED`。

[UNKNOWN] object segmentation、threshold、connected-component rules、minimum area 和 tracking association 尚未定义。在这些定义固定前，不得提出 object claim。
