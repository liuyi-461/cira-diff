# Pixel Metrics

## 候选指标

- mean error / bias；
- MAE；
- MSE 和 RMSE；
- 只有在 normalized BT 的 dynamic-range convention 合理时，才考虑 PSNR。

## 状态

当前项目为 `NOT IMPLEMENTED`。论文中的使用属于 `[EVIDENCE]`，不是本地 result。

## Protocol 字段

必须说明 inverse-normalized 或 normalized units、valid-pixel mask、per-frame/pooled reduction、sample weighting、lead times，以及 ensemble reduction。
