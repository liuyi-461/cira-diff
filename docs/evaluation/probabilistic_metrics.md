# Probabilistic Metrics

候选指标包括 CRPS、Brier score、reliability diagrams、spread-skill relationship、rank histograms 和 sharpness/coverage diagnostics。

状态：`PLANNED`。

每个 ensemble result 必须记录 member count、random seeds、sampler settings、conditioning、ensemble mean/median 处理方式，以及是否匹配 compute budget。
