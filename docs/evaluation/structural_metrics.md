# Structural Metrics

候选指标包括 SSIM、multi-scale SSIM、经过 domain-applicability audit 的 LPIPS，以及 edge/gradient error 和 structure-aware correlation。

状态：`PLANNED / UNDER EVALUATION`。Generic perceptual metrics 不能默认代表 cloud physics 或 forecast skill。
